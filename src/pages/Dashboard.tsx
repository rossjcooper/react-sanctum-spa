import AppLayout from "../layouts/AppLayout";

export const Dashboard = () => {
    return (
       <AppLayout header={
		   <div>Dashboard</div>
	   }>
		   <div>

		   </div>
	   </AppLayout>
    );
}

export default Dashboard;